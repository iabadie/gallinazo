# gallinazo

git clone https://github.com/iabadie/gallinazo

cd gallinazo/

git remote

git remote -v

git checkout -b NAME

git status

git add --all git add .

git commit -m "COMENTARIO"

git push origin HEAD
